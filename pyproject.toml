[project]
name = "jepa-collapse-diagnostics"
version = "0.1.0"
description = "Small PyTorch experiment for measuring representation collapse"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.14.0",
    "torchvision>=0.29.0",
]
